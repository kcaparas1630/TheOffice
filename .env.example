# Written automatically to .env.local by `npx convex dev` (anonymous local mode):
#   CONVEX_DEPLOYMENT=...
#   NEXT_PUBLIC_CONVEX_URL=http://127.0.0.1:3210

# LLM access — set this ON THE CONVEX DEPLOYMENT (not just in this file):
#   npx convex env set OPENROUTER_API_KEY sk-or-...
OPENROUTER_API_KEY=

# Optional model override (default: anthropic/claude-haiku-4.5). Also set via convex env.
# OPENROUTER_MODEL=anthropic/claude-haiku-4.5
