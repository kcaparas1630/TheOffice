export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4 font-mono">
      <h1 className="text-2xl font-bold">🏢 The Office</h1>
      <p className="text-sm opacity-70">
        v1 is headless. Run <code className="font-bold">npm run office</code> in a terminal.
      </p>
      <p className="text-xs opacity-50">The pixel office lives here later.</p>
    </main>
  );
}
